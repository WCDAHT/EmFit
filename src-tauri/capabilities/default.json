{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main window. STANDARDS §3.5: least privilege. Add a permission only when a feature needs it; never grant a plugin's full surface (e.g. unrestricted fs) by reflex.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
