# Screen-level, app-specific views (e.g. ConversationListView.svelte,
# SettingsDialog.svelte). See STANDARDS sec 3.1. Reusable pieces go in
# ../components/ instead; promote to a component on the second actual use.
